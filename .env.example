POSTGRES_USER=fairtix_user
POSTGRES_PASSWORD=changeme
POSTGRES_DB=fairtix
POSTGRES_HOST=postgres
POSTGRES_PORT=5432

REDIS_HOST=redis
REDIS_PORT=6379

# Generate with: python3 -c "import secrets; print(secrets.token_hex(48))"
JWT_SECRET=replace_with_a_secret_at_least_64_characters_long

MAIL_HOST=mailhog
MAIL_PORT=1025
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_AUTH=false
MAIL_STARTTLS=false
MAIL_FROM=noreply@fairtix.com
APP_BASE_URL=http://localhost:3000

# reCAPTCHA v2 (https://www.google.com/recaptcha/admin) — set RECAPTCHA_ENABLED=true and fill in secret
RECAPTCHA_ENABLED=false
RECAPTCHA_SECRET=

# Stripe — set STRIPE_ENABLED=true and fill in test keys from dashboard.stripe.com
STRIPE_ENABLED=false
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...
